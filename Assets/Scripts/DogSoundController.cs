using UnityEngine;
using System.Collections;

public class DogSoundController : MonoBehaviour {

    //public AudioClip dogBarkClip;

    //AudioSource dogBarkSource;

    //GameObject mainCamera;
    
    // Use this for initialization
	void Start () 
    {
        //mainCamera = GameObject.FindGameObjectWithTag("MainCamera");
        
        //dogBarkSource.clip = dogBarkClip;
	}
	
	// Update is called once per frame
	void LateUpdate () 
    {
        //if (mainCamera.transform.position.x >= (transform.position.x + 10) && mainCamera.transform.position.x <= (transform.position.x - 10))
        //    gameObject.audio.Play();
            //dogBarkSource.Play();
	}
}
